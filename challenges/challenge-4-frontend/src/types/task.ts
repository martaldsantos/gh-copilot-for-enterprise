// TODO: Use Copilot to define TypeScript types for the Task Dashboard
// Ask: "Create TypeScript interfaces for Task, User, TaskFilters, and API responses"

// Example structure to get you started:
export interface Task {
  // TODO: Add properties
  // id, title, description, status, priority, dueDate, createdAt, etc.
}

export enum TaskStatus {
  // TODO: Define status values
}

export enum TaskPriority {
  // TODO: Define priority values
}
