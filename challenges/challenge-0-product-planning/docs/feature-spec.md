# Feature Specification Template

Copy this file once per feature. Write two feature specs total. One of the two
must be for a feature that builds on the existing REST API -- map its requirements
to specific existing endpoints and identify what API changes are needed.

---

## Feature Name

## Overview

## User Personas Affected

## User Flow

Describe step-by-step interactions. Include a Mermaid **sequence diagram** showing
system interactions and a Mermaid **state diagram** showing the primary entity's
lifecycle.

## Requirements

### Functional Requirements

### Non-Functional Requirements

Every NFR must have a specific, testable threshold with units (e.g., "p95 latency
< 3 seconds" not "should be fast").

## API Alignment (if applicable)

Map this feature's needs to the existing REST API endpoints. List which endpoints
exist today, which need modifications, and which are entirely new. Document these
as API Change Requests.

## Out of Scope

## Open Questions

## Wireframes / Mockups

## Dependencies

## Acceptance Criteria
