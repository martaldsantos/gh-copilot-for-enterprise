# TODO: Use Copilot to create a Service
# Ask: "Create a Kubernetes Service to expose the quote-app on port 80 targeting port 3000"

apiVersion: v1
kind: Service
metadata:
  name: quote-service
spec:
  # TODO: Define selector matching the deployment
  # TODO: Define ports (port 80 -> targetPort 3000)
  # TODO: Set type to LoadBalancer
